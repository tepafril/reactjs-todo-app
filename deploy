git add .
git commit -m "Commiting before deploy"
git push
rm -rf build
npm run build
cp -r build/* ./
git add .
git stash
git checkout gh-pages
rm -rf !(node_modules)
git stash apply
git add -u .
git commit -m "deploy"
git push --force
git checkout main
