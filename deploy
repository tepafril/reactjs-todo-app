git add .
git commit -m "Commiting before deploy"
git push
rm -rf /build
npm run build
cp -r build/* ./
git add .
git stash
git checkout gh-pages
rm -rf *
rm index.html
rm asset-manifest.json
git stash apply
git commit -m "deploy"
git push
git checkout main
